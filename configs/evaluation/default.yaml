# How a trained model's latent space is measured against the feature classes.

# The split the latents are read from: train or validation.
split: validation

# How many nearest latents one retrieval reads, the paper's top ten.
neighbours: 10

# The published model to read, or nothing for the latest this architecture published.
model: null
