# How a trained model's latent space is read. Nothing is measured over it yet:
# a tile is matched against a similarity between geological features, and that
# dataset is not built.

# The split the latents are read from: train or validation.
split: validation
