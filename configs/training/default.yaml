# How a run trains, validates and stops.

# A split holds a few hundred features, so an epoch is a handful of steps.
epochs: 1000
# How many tiles one step reads.
batch_size: 32
learning_rate: 0.00015
weight_decay: 0.05
warmup_epochs: 10
patience: 25

# The share of each instrument's patches hidden from its encoder, drawn independently.
mask_ratio: 0.5

# Where checkpoints land, relative to the repository.
checkpoints: checkpoints
